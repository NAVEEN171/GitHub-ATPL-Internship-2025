import { CountDown } from './count-down';

describe('CountDown', () => {
  it('should create an instance', () => {
    const directive = new CountDown();
    expect(directive).toBeTruthy();
  });
});
