import { DisableBasedCondition } from './disable-based-condition';

describe('DisableBasedCondition', () => {
  it('should create an instance', () => {
    const directive = new DisableBasedCondition();
    expect(directive).toBeTruthy();
  });
});
