import { AppResizeTracker } from './app-resize-tracker';

describe('AppResizeTracker', () => {
  it('should create an instance', () => {
    const directive = new AppResizeTracker();
    expect(directive).toBeTruthy();
  });
});
