import { Routes } from '@angular/router';
import { ElementResizeListener } from './components/element-resize-listener/element-resize-listener';

export const routes: Routes = [{ path: '', component: ElementResizeListener }];
